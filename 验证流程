### 🔥 最简验证流程（按功能模块逐一验证，新手友好）
核心原则：**先验证基础登录，再验证核心操作，最后验证权限控制**，每个步骤都标注「操作+成功/失败标志」，确保快速定位问题。

---

### 一、功能1：用户注册与登录（JWT身份认证）
#### 操作步骤
1. **注册验证**（可选，若已有测试账号可跳过）：
   - 访问注册页：`http://localhost:8080/register.html`；
   - 输入用户名（如test002）、密码（如123456），点击注册；
2. **登录验证**：
   - 访问登录页：`http://localhost:8080/login.html`；
   - 输入test001/123456，点击登录；
3. **JWT验证**：
   - 登录后按F12 → 「Application」→「LocalStorage」→`http://localhost:8080`，查看是否有`token`/`username`/`userId`字段；
   - 复制`token`，访问需要认证的接口（如`/book/list`），在请求头加`token: 你的Token`，看是否能正常返回数据。

#### 成功标志
- 注册：数据库`user`表新增test002数据，无报错；
- 登录：弹窗「登录成功」，跳转到`index.html`，LocalStorage有Token；
- JWT认证：带Token访问接口返回200，不带Token返回401/未登录提示。

#### 失败排查
- 登录无响应：检查登录页Console是否有JS语法错误；
- 登录后无Token：检查后端`UserController`的`login`接口是否返回`res.data=Token`；
- 带Token仍401：检查`JwtInterceptor`是否正确读取`token`请求头。

---

### 二、功能2：商品管理（发布/浏览/编辑/删除）
#### 操作步骤
1. **发布商品**：
   - 登录后进入`index.html`，填写书名（如Java编程思想）、作者、价格、成色，点击「发布」；
2. **浏览商品**：
   - 发布后查看页面右侧「书籍列表」，是否显示刚发布的商品；
3. **编辑商品**（需后端`/book/update/{id}`接口）：
   - 找到商品列表中的目标商品，点击「编辑」，修改价格（如25.9），提交；
4. **删除商品**（需后端`/book/delete/{id}`接口）：
   - 点击商品后的「删除」按钮，确认删除。

#### 成功标志
- 发布：数据库`book`表新增数据，`userId`匹配当前登录用户，列表刷新显示该商品；
- 浏览：列表正确渲染商品名称/作者/价格，无`forEach`报错；
- 编辑：数据库`book`表对应商品价格更新，列表同步刷新；
- 删除：数据库`book`表对应商品被删除，列表中消失。

#### 失败排查
- 发布报401：检查`index.html`的发布请求是否加了`token`请求头；
- 发布成功但列表不显示：检查`loadBooks()`函数是否正确拼接HTML；
- 编辑/删除无权限：检查后端是否校验「商品所属用户ID=当前登录用户ID」。

---

### 三、功能3：商品分类/关键词搜索/分页展示
#### 操作步骤
1. **分类查询**（需后端`/book/list?category=xxx`接口）：
   - 在商品列表页选择分类（如「编程」），点击查询；
2. **关键词搜索**（需后端`/book/search?keyword=xxx`接口）：
   - 输入关键词（如「Java」），点击搜索；
3. **分页展示**（需后端`/book/list?page=1&size=5`接口）：
   - 发布超过5个商品，点击「下一页」，查看是否显示第2页数据。

#### 成功标志
- 分类：列表只显示对应分类的商品（如选「编程」只显示编程类书籍）；
- 搜索：列表只显示名称/作者包含关键词的商品；
- 分页：每页显示指定数量（如5条），页码切换正常，无数据重复/丢失。

#### 失败排查
- 分类/搜索无效果：检查后端接口是否接收`category`/`keyword`参数并拼接查询条件；
- 分页失效：检查后端是否使用MyBatis-Plus的分页插件，返回`total`/`pages`字段。

---

### 四、功能4：下单购买+查看个人订单
#### 操作步骤
1. **下单购买**（需后端`/order/create`接口）：
   - 在商品列表点击「购买」按钮，确认下单；
2. **查看个人订单**（需后端`/order/my/{userId}`接口）：
   - 进入「我的订单」页面，查看是否显示刚下单的订单。

#### 成功标志
- 下单：数据库`order_info`表新增订单数据，`userId`/`bookId`匹配，`orderNo`唯一；
- 查看订单：列表显示当前用户的所有订单，无他人订单。

#### 失败排查
- 下单失败：检查订单接口是否加`token`请求头，`userId`是否正确传递；
- 查看他人订单：检查后端是否校验「订单所属用户ID=当前登录用户ID」。

---

### 五、功能5：角色权限控制（普通用户vs管理员）
#### 操作步骤
1. **准备两个账号**：
   - 普通用户：`test001`（`role=user`）；
   - 管理员：`admin`（`role=admin`）；
2. **普通用户操作**：
   - 登录test001，访问管理员接口（如`/admin/book/list`），看是否返回「无权限」；
3. **管理员操作**：
   - 登录admin，访问`/admin/book/list`，看是否显示所有商品；
   - 尝试删除其他用户发布的商品，看是否成功。

#### 成功标志
- 普通用户：无法访问`/admin/*`接口，无法删除他人商品；
- 管理员：可访问所有`/admin/*`接口，可管理所有商品/用户。

#### 失败排查
- 权限失效：检查后端是否从Token解析`role`字段，是否在接口上加角色校验（如`if(!"admin".equals(role)){return R.fail("无权限")}`）。

---

### 六、功能6：管理员管理（商品/用户）
#### 操作步骤
1. **管理员管理商品**：
   - 登录admin，进入「商品管理」页面，删除违规商品/修改商品状态；
2. **管理员管理用户**（需后端`/admin/user/list`/`/admin/user/delete/{id}`接口）：
   - 查看所有用户列表，删除违规用户（如test002）。

#### 成功标志
- 管理商品：任意商品可被删除/下架，数据库同步更新；
- 管理用户：违规用户被删除，`user`表对应数据消失，关联的`book`/`order`可按需删除。

---

### 🎯 快速验证技巧（节省时间）
1. **控制台日志**：所有操作前按F12，「Network」标签查看请求/响应，「Console」标签看报错；
2. **数据库校验**：用SQL快速核对数据：
   - 注册/登录：`select * from user where username='test001';`
   - 发布商品：`select * from book where user_id=1;`
   - 下单：`select * from order_info where user_id=1;`
3. **核心接口测试**（Postman快捷测试）：
   | 接口         | 请求方式 | 地址                | 必要请求头       |
   |--------------|----------|---------------------|------------------|
   | 登录         | POST     | `/user/login`       | 无               |
   | 发布商品     | POST     | `/book/add`         | `token: xxx`     |
   | 我的订单     | GET      | `/order/my/1`       | `token: xxx`     |
   | 管理员商品列表 | GET     | `/admin/book/list`  | `token: xxx`     |

---

### ✅ 最终验证通过标准
1. 所有操作无JS语法错误、无401/404/500接口错误；
2. 数据库数据与操作一致（增/删/改后同步更新）；
3. 权限控制精准：普通用户/管理员只能操作对应权限的功能；
4. 核心流程闭环：注册→登录→发布商品→下单→查看订单→管理员管理，全流程无卡顿。