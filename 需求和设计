# 校园二手书交易系统——需求分析+总体设计（直接可粘贴到试卷）
## 一、需求分析
### 1. 功能需求
- **用户模块**：注册、登录、JWT身份验证、个人信息修改
- **书籍模块**：书籍发布、编辑、删除、分类查询、搜索、详情查看
- **交易模块**：下单、我的订单、订单状态修改（待付款/已付款/已完成/已取消）
- **权限模块**：普通用户/管理员（RBAC简化版）
- **基础功能**：RESTful接口、数据校验、异常处理

### 2. 用户需求
- 学生：快速发布二手书、按书名/分类找书、安全下单
- 管理员：管理用户、审核书籍、处理异常订单

### 3. 性能需求
- 接口响应≤500ms
- 支持200人同时在线使用
- 数据稳定不丢失、操作可回滚

---

## 二、总体设计
### 1. 系统架构设计
**后端**：Spring Boot + MyBatis-Plus + JWT + RBAC + MySQL
**接口**：RESTful API
**分层**：Controller → Service → Mapper → DB

### 2. 模块划分
1. 用户管理模块
2. 书籍管理模块
3. 订单交易模块
4. 权限控制模块
5. 公共工具模块（JWT、异常、响应）

### 3. 数据库设计（最简5表，新手必过）
1. **user（用户表）**
id、username、password、nickname、phone、role、create_time

2. **book（书籍表）**
id、name、author、isbn、price、old_level、category、user_id、status、create_time

3. **category（分类表）**
id、name、sort

4. **order（订单表）**
id、order_no、user_id、book_id、price、status、pay_time、create_time

5. **role（角色表，简化）**
id、role_name、remark

---

## 三、详细设计（接口设计·核心接口）
- POST /user/register 注册
- POST /user/login 登录
- GET /book/list 书籍列表
- GET /book/search 搜索
- POST /book/add 发布书籍
- POST /order/create 创建订单
- GET /order/list 我的订单
- GET /admin/user/list 用户管理（管理员）

---

需要我继续帮你写**完整报告正文**（引言、技术概述、详细设计、测试、总结、参考文献）吗？直接可交。